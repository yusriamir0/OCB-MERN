const express = require("express"); // express is similar to a function

const app = express();

app.listen(8080, function () {
    console.log("Server is running at port 8080");
});

// https://localhost:8080
